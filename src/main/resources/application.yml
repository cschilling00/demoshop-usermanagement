server:
  port : 8081

spring:
  data:
    mongodb:
      port: 27017
      database: GraphqlDemoShopDb
      host: localhost

logging:
  level:
    graphql:
      GraphQL: DEBUG
    org:
      springframework:
        web: TRACE
        security: DEBUG

